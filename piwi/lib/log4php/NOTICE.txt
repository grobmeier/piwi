The log4php is an Apache project is currently incubating at Apache Incubator.
We do not do any fixes in our api version but try to forward patches to the apache
project if necessary. Please notice that we have only included the classes
we need for the use with PIWI. The complete package and source can be found
at the log4php website.

Incubator site of log4php:
http://incubator.apache.org/projects/log4php.html

Official project website:
http://incubator.apache.org/log4php/

DIFFERENCES FROM ORIGINAL CODE:

- Fixes: Issue 7: log4php.dtd is invalid. category, categoryLogger and errorHandler dropped out. They cannot be implemented with PHP4. Patch is supplied to log4php team.